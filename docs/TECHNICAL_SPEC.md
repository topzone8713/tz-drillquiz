# DrillQuiz - 기술 명세서

## 📋 시스템 아키텍처

### 기술 스택
- **Backend**: Python 3.12, Django 4.2.7, Django REST Framework 3.14.0
- **Frontend**: Vue.js 2.6.14, Bootstrap 5.3.2, Vue i18n 8.28.2
- **Database**: PostgreSQL 15 (운영), SQLite3 (개발)
- **Infrastructure**: Docker, Kubernetes, Jenkins CI/CD
- **Storage**: MinIO S3 호환 스토리지 (운영), 로컬 파일 시스템 (개발)
- **Cache**: Redis Cluster (운영), 로컬 메모리 캐시 (개발)

### 배포 환경
- **개발 환경**: devops-dev 네임스페이스
- **운영 환경**: devops 네임스페이스
- **도메인**: us.drillquiz.com
- **CI/CD**: Jenkins 파이프라인

---

## 📊 데이터 모델 명세

### 1. 사용자 관리 (User Management)

#### UserProfile 모델
```python
- user: OneToOneField(User) - Django 기본 사용자와 연결
- role: CharField - 사용자 역할 (admin_role, study_admin_role, user_role)
- language: CharField - 언어 설정 (ko, en)
- email_verified: BooleanField - 이메일 인증 상태
- random_exam_email_enabled: BooleanField - 랜덤 출제 이메일 설정
- random_exam_question_count: IntegerField - 랜덤 출제 문제 수
- retention_cleanup_enabled: BooleanField - 자동 정리 활성화
- retention_cleanup_percentage: IntegerField - 자동 정리 비율
```

#### 요구사항
- **다국어 지원**: 사용자별 언어 설정 저장 및 적용
- **역할 기반 접근 제어**: 관리자, 스터디 관리자, 일반 사용자 구분
- **이메일 인증**: Google OAuth 및 이메일 인증 지원
- **개인화 설정**: 랜덤 출제, 자동 정리 등 개인 설정 관리

### 2. 문제 관리 (Question Management)

#### Question 모델
```python
- id: UUIDField - 고유 식별자
- csv_id: CharField - CSV 문제 ID
- source_id: CharField - 출처 파일명
- title_ko/title_en: CharField - 다국어 제목
- content_ko/content_en: TextField - 다국어 문제 내용
- answer_ko/answer_en: CharField - 다국어 정답
- explanation_ko/explanation_en: TextField - 다국어 설명
- difficulty: CharField - 난이도
- group_id: CharField - 그룹 ID
- created_language: CharField - 생성 언어
- is_ko_complete/is_en_complete: BooleanField - 언어별 완성도
- created_by: ForeignKey - 생성자
```

#### 요구사항
- **다국어 콘텐츠**: 한국어/영어 제목, 내용, 정답, 설명 지원
- **문제 분류**: 난이도, 그룹 ID를 통한 문제 분류
- **완성도 추적**: 언어별 콘텐츠 완성도 자동 계산
- **출처 관리**: 문제 출처 파일 추적
- **권한 관리**: 생성자별 문제 소유권 관리

### 3. 시험 관리 (Exam Management)

#### Exam 모델
```python
- id: UUIDField - 고유 식별자
- title_ko/title_en: CharField - 다국어 제목
- description_ko/description_en: TextField - 다국어 설명
- questions: ManyToManyField - 문제들 (ExamQuestion 통해 연결)
- total_questions: IntegerField - 총 문제 수
- is_public: BooleanField - 공개 여부
- force_answer: BooleanField - 답안 입력 강제
- original_exam: ForeignKey - 원본 시험 (재시험용)
- version_number: IntegerField - 버전 번호
- is_original: BooleanField - 원본 여부
- created_by: ForeignKey - 생성자
```

#### 요구사항
- **다국어 시험**: 한국어/영어 제목 및 설명 지원
- **버전 관리**: 원본 시험과 재시험 버전 구분
- **공개/비공개**: 시험 공개 여부 설정
- **답안 검증**: 답안 입력 강제 옵션
- **문제 연결**: Many-to-Many 관계로 문제 연결

### 4. 시험 결과 관리 (Exam Results)

#### ExamResult 모델
```python
- id: UUIDField - 고유 식별자
- exam: ForeignKey - 시험
- user: ForeignKey - 사용자
- score: IntegerField - 점수
- total_score: IntegerField - 총점
- correct_count: IntegerField - 정답 수
- wrong_count: IntegerField - 오답 수
- completed_at: DateTimeField - 완료일
- elapsed_seconds: IntegerField - 소요 시간
```

#### ExamResultDetail 모델
```python
- result: ForeignKey - 시험 결과
- question: ForeignKey - 문제
- question_title: CharField - 문제 제목 (보존용)
- question_content: TextField - 문제 내용 (보존용)
- question_answer: CharField - 문제 정답 (보존용)
- user_answer: CharField - 사용자 답안
- is_correct: BooleanField - 정답 여부
- elapsed_seconds: IntegerField - 문제별 소요 시간
```

#### 요구사항
- **상세한 결과 추적**: 문제별 정답/오답 기록
- **통계 정보 보존**: 문제 삭제 후에도 통계 정보 유지
- **시간 측정**: 시험 전체 및 문제별 소요 시간 기록
- **개인 통계**: 사용자별 학습 진도 및 성취도 추적

### 5. 스터디 관리 (Study Management)

#### Study 모델
```python
- title_ko/title_en: CharField - 다국어 제목
- goal_ko/goal_en: TextField - 다국어 목표
- start_date/end_date: DateField - 시작/종료일
- is_public: BooleanField - 공개 여부
- created_by: ForeignKey - 생성자
- created_language: CharField - 생성 언어
- is_ko_complete/is_en_complete: BooleanField - 언어별 완성도
```

#### StudyTask 모델
```python
- study: ForeignKey - 스터디
- name_ko/name_en: CharField - 다국어 Task 이름
- exam: ForeignKey - 연결된 시험
- progress: FloatField - 진행률 (0-100%)
- seq: IntegerField - 순서
- is_public: BooleanField - 공개 여부
```

#### Member 모델
```python
- study: ForeignKey - 스터디
- user: ForeignKey - 가입된 사용자
- name: CharField - 이름
- email: EmailField - 이메일
- member_id: CharField - 사용자 정의 ID
- affiliation: CharField - 소속
- location: CharField - 위치
- role: CharField - 스터디 내 역할
- is_active: BooleanField - 활성화 상태
```

#### 요구사항
- **다국어 스터디**: 한국어/영어 제목 및 목표 지원
- **Task 기반 학습**: 시험을 Task로 연결하여 체계적 학습
- **멤버 관리**: 스터디 멤버 초대, 역할 관리
- **진행률 추적**: Task별 학습 진행률 자동 계산
- **가입 요청**: StudyJoinRequest를 통한 스터디 가입 프로세스

---

## 🎯 핵심 기능 명세

### 1. 사용자 인증 및 권한 관리

#### Google OAuth 인증
- **요구사항**: Google 계정을 통한 소셜 로그인
- **구현**: Google OAuth 2.0 플로우
- **보안**: CSRF 토큰, 세션 관리
- **다국어**: 사용자 프로필 언어 설정 자동 적용

#### 역할 기반 접근 제어
- **관리자 (admin_role)**: 전체 시스템 관리 권한
- **스터디 관리자 (study_admin_role)**: 스터디 생성 및 관리 권한
- **일반 사용자 (user_role)**: 기본 학습 기능 사용 권한

### 2. 문제 관리 시스템

#### 문제 업로드 및 관리
- **파일 형식**: Excel (.xlsx), CSV 파일 지원
- **다국어 지원**: 한국어/영어 콘텐츠 자동 번역
- **문제 분류**: 난이도, 그룹 ID를 통한 분류
- **공개/비공개**: 문제 파일별 공개 여부 설정

#### 문제 풀이 기능
- **즐겨찾기**: 개인별 즐겨찾기 문제 관리
- **무시 기능**: 개인별 문제 무시 설정
- **랜덤 출제**: 설정된 문제 수만큼 랜덤 출제
- **일일 시험**: 구독한 시험에서 매일 새로운 문제 출제

### 3. 시험 시스템

#### 시험 생성 및 관리
- **스마트 출제**: 필터된 문제들만으로 시험 생성
- **재시험**: 틀린 문제만으로 재시험 생성
- **버전 관리**: 원본 시험과 재시험 버전 구분
- **공개/비공개**: 시험 공개 여부 설정

#### 시험 풀이
- **실시간 채점**: 답안 제출 시 즉시 채점
- **시간 측정**: 시험 전체 및 문제별 소요 시간 기록
- **진행률 표시**: 현재 문제 위치 및 남은 문제 수 표시
- **답안 검증**: 답안 입력 강제 옵션

### 4. 스터디 그룹 시스템

#### 스터디 생성 및 관리
- **다국어 스터디**: 한국어/영어 제목 및 목표 설정
- **Task 기반 학습**: 시험을 Task로 연결하여 체계적 학습
- **진행률 추적**: Task별 학습 진행률 자동 계산
- **멤버 관리**: 스터디 멤버 초대 및 역할 관리

#### 협력 학습
- **가입 요청**: StudyJoinRequest를 통한 스터디 가입
- **진행률 공유**: 스터디 멤버 간 학습 진행률 공유
- **대시보드**: 스터디 전체 진행률 시각화

### 5. 다국어 지원 시스템

#### 자동 번역
- **Vue.js 번역**: `$t('key')` 함수를 통한 프론트엔드 번역
- **Django 번역**: `.po` 파일을 통한 백엔드 번역
- **자동화**: Vue 파일에서 번역 키 자동 추출 및 Django 동기화

#### 번역 관리
- **번역 키 추출**: `npm run i18n:extract`
- **번역 컴파일**: `npm run i18n:compile`
- **전체 동기화**: `npm run i18n:sync`

### 6. 데이터 관리 및 통계

#### 통계 시스템
- **개인 통계**: 사용자별 학습 진도 및 성취도
- **문제 통계**: 문제별 정답률 및 시도 횟수
- **시험 통계**: 시험별 평균 점수 및 완료율
- **스터디 통계**: 스터디별 전체 진행률

#### 데이터 보존
- **통계 정보 보존**: 문제 삭제 후에도 통계 정보 유지
- **백업 시스템**: 정기적인 데이터 백업
- **자동 정리**: 설정된 비율에 따른 성공 기록 자동 삭제

---

## 🔧 기술적 요구사항

### 1. 성능 최적화

#### 데이터베이스 최적화
- **인덱스**: 자주 사용되는 필드에 대한 복합 인덱스
- **쿼리 최적화**: N+1 문제 해결을 위한 select_related, prefetch_related
- **캐싱**: Redis를 통한 자주 사용되는 데이터 캐싱

#### 프론트엔드 최적화
- **번들 최적화**: Vue.js 프로덕션 빌드 최적화
- **이미지 최적화**: WebP 형식 지원
- **CDN**: 정적 파일 CDN 배포

### 2. 보안 요구사항

#### 인증 및 권한
- **CSRF 보호**: Django CSRF 토큰을 통한 요청 검증
- **세션 관리**: 안전한 세션 생성 및 관리
- **권한 검증**: API 엔드포인트별 권한 검증

#### 데이터 보호
- **개인정보 보호**: 개인 통계만 반환, 전체 통계 노출 방지
- **데이터 암호화**: 민감한 데이터 암호화 저장
- **접근 제어**: 역할 기반 데이터 접근 제어

### 3. 확장성 요구사항

#### 마이크로서비스 아키텍처
- **API 분리**: RESTful API를 통한 프론트엔드/백엔드 분리
- **서비스 분리**: 인증, 문제 관리, 시험 시스템 독립적 운영

#### 클라우드 네이티브
- **컨테이너화**: Docker를 통한 애플리케이션 컨테이너화
- **오케스트레이션**: Kubernetes를 통한 컨테이너 오케스트레이션
- **자동 스케일링**: HPA를 통한 자동 수평 확장

### 4. 모니터링 및 로깅

#### 로깅 시스템
- **구조화된 로깅**: JSON 형식의 구조화된 로그
- **로그 레벨**: DEBUG, INFO, WARNING, ERROR 레벨 구분
- **로그 수집**: 중앙화된 로그 수집 및 분석

#### 모니터링
- **헬스 체크**: `/api/health/` 엔드포인트를 통한 서비스 상태 확인
- **메트릭 수집**: CPU, 메모리, 디스크 사용률 모니터링
- **알림 시스템**: 서비스 장애 시 자동 알림

---

## 🚀 배포 및 운영 명세

### 1. CI/CD 파이프라인

#### Jenkins 파이프라인
- **소스 코드 체크아웃**: Git 저장소에서 최신 코드 가져오기
- **프론트엔드 빌드**: Vue.js 프로덕션 빌드
- **Docker 이미지 빌드**: 백엔드 애플리케이션 컨테이너화
- **Kubernetes 배포**: 자동화된 K8s 배포

#### 환경별 배포
- **개발 환경**: devops-dev 네임스페이스
- **운영 환경**: devops 네임스페이스
- **브랜치별 배포**: main 브랜치는 운영, dev 브랜치는 개발

### 2. 인프라 요구사항

#### Kubernetes 클러스터
- **네임스페이스**: devops, devops-dev
- **리소스 제한**: CPU 500m-1000m, 메모리 500Mi-1000Mi
- **자동 스케일링**: HPA를 통한 2-10개 레플리카 자동 조정

#### 스토리지
- **데이터베이스**: PostgreSQL 15 클러스터
- **파일 스토리지**: MinIO S3 호환 스토리지
- **캐시**: Redis 클러스터

#### 네트워킹
- **Ingress**: Nginx Ingress Controller
- **SSL/TLS**: Let's Encrypt 인증서 자동 갱신
- **도메인**: us.drillquiz.com

### 3. 운영 요구사항

#### 백업 및 복구
- **데이터베이스 백업**: 정기적인 PostgreSQL 백업
- **파일 백업**: MinIO 스토리지 백업
- **복구 절차**: 장애 시 빠른 복구 절차

#### 보안 관리
- **시크릿 관리**: Kubernetes Secret을 통한 민감한 정보 관리
- **접근 제어**: RBAC를 통한 사용자 권한 관리
- **네트워크 보안**: 네트워크 정책을 통한 트래픽 제어

---

## 📱 사용자 인터페이스 명세

### 1. 반응형 디자인

#### 디바이스 지원
- **데스크톱**: 1920x1080 이상 해상도
- **태블릿**: 768x1024 해상도
- **모바일**: 375x667 이상 해상도

#### UI/UX 요구사항
- **Bootstrap 5**: 반응형 그리드 시스템
- **모바일 최적화**: 터치 친화적 인터페이스
- **접근성**: WCAG 2.1 AA 수준 준수

### 2. 다국어 인터페이스

#### 언어 지원
- **한국어**: 기본 언어
- **영어**: 글로벌 사용자 지원
- **동적 언어 변경**: 실시간 언어 전환

#### 번역 관리
- **자동 추출**: Vue 파일에서 번역 키 자동 추출
- **번역 동기화**: 프론트엔드/백엔드 번역 동기화
- **번역 완성도**: 언어별 번역 완성도 추적

### 3. 사용자 경험

#### 직관적 네비게이션
- **메뉴 구조**: 홈, 스터디 관리, 시험 관리
- **브레드크럼**: 현재 위치 표시
- **검색 기능**: 문제, 시험, 스터디 검색

#### 피드백 시스템
- **로딩 상태**: 작업 진행 상태 표시
- **에러 메시지**: 명확한 에러 메시지 제공
- **성공 알림**: 작업 완료 시 성공 메시지

---

## 🔍 품질 보증 명세

### 1. 테스트 요구사항

#### 단위 테스트
- **백엔드**: Django 모델, 뷰, 시리얼라이저 테스트
- **프론트엔드**: Vue 컴포넌트 단위 테스트
- **코드 커버리지**: 80% 이상 커버리지 목표

#### 통합 테스트
- **API 테스트**: REST API 엔드포인트 테스트
- **데이터베이스 테스트**: 데이터베이스 연동 테스트
- **인증 테스트**: OAuth 인증 플로우 테스트

#### 성능 테스트
- **로드 테스트**: 동시 사용자 100명 이상 지원
- **응답 시간**: API 응답 시간 2초 이내
- **데이터베이스 성능**: 복잡한 쿼리 최적화

### 2. 코드 품질

#### 코딩 표준
- **Python**: PEP 8 스타일 가이드 준수
- **JavaScript**: ESLint 규칙 준수
- **Vue.js**: Vue.js 스타일 가이드 준수

#### 코드 리뷰
- **Pull Request**: 모든 코드 변경사항 리뷰
- **자동화**: CI/CD 파이프라인에서 자동 검증
- **문서화**: 코드 변경사항 문서화

### 3. 보안 테스트

#### 취약점 스캔
- **의존성 스캔**: 보안 취약점이 있는 패키지 검사
- **코드 스캔**: 정적 코드 분석을 통한 보안 취약점 검사
- **침투 테스트**: 외부 보안 전문가를 통한 침투 테스트

---

## 📈 성능 및 확장성 명세

### 1. 성능 목표

#### 응답 시간
- **API 응답**: 평균 500ms 이내
- **페이지 로딩**: 초기 로딩 3초 이내
- **데이터베이스 쿼리**: 복잡한 쿼리 1초 이내

#### 처리량
- **동시 사용자**: 100명 이상 동시 접속 지원
- **API 요청**: 초당 100개 요청 처리
- **데이터베이스**: 초당 1000개 쿼리 처리

### 2. 확장성 계획

#### 수평 확장
- **마이크로서비스**: 서비스별 독립적 확장
- **데이터베이스 샤딩**: 데이터 분산을 통한 성능 향상
- **캐시 클러스터**: Redis 클러스터를 통한 캐시 확장

#### 수직 확장
- **리소스 증가**: CPU, 메모리 리소스 증설
- **스토리지 확장**: 데이터베이스 스토리지 확장
- **네트워크 대역폭**: 네트워크 대역폭 증설

### 3. 성능 최적화 구현

#### API 최적화
- **시험 결과 API**: 필요한 필드만 조회하여 50-70% 데이터 크기 감소
- **배치 번역**: 개별 번역 대신 배치 처리로 20-40배 성능 향상
- **쿼리 최적화**: `select_related`, `only()` 사용으로 불필요한 데이터 제거

#### 번역 시스템 최적화
- **배치 번역**: 200개 문제를 50개씩 배치로 분할하여 처리 시간 1-2분으로 단축
- **공통 모듈화**: `MultilingualContentManager`로 다국어 처리 로직 통합
- **메모리 최적화**: 배치 처리 후 `gc.collect()` 호출로 메모리 정리

#### 데이터베이스 최적화
- **인덱스 최적화**: 자주 사용되는 필드에 대한 복합 인덱스
- **쿼리 최적화**: N+1 문제 해결을 위한 `select_related`, `prefetch_related`
- **캐싱**: Redis를 통한 자주 사용되는 데이터 캐싱

---

## 🛠️ 개발 및 유지보수 명세

### 1. 개발 환경

#### 로컬 개발
- **Docker Compose**: 원클릭 로컬 환경 구축
- **Hot Reload**: 코드 변경 시 자동 재시작
- **디버깅**: IDE 통합 디버깅 지원

#### 개발 도구
- **IDE**: VS Code, PyCharm 지원
- **버전 관리**: Git을 통한 소스 코드 관리
- **이슈 추적**: GitHub Issues를 통한 이슈 관리

### 2. 유지보수

#### 모니터링
- **애플리케이션 모니터링**: APM 도구를 통한 성능 모니터링
- **인프라 모니터링**: 서버 리소스 모니터링
- **로그 분석**: 중앙화된 로그 분석

#### 업데이트
- **의존성 업데이트**: 정기적인 패키지 업데이트
- **보안 패치**: 보안 취약점 패치
- **기능 업데이트**: 사용자 요청에 따른 기능 추가

---

## 🚨 운영 가이드

### 1. 다국어 마이그레이션 가이드

#### ⚠️ 중요: 운영 환경 적용 전 필수 체크리스트

##### 사전 준비 (적용 전 24시간)
- [ ] **전체 데이터베이스 백업**: `pg_dump -U admin -d drillquiz > backup_$(date +%Y%m%d_%H%M%S).sql`
- [ ] **백업 파일 검증**: 백업 파일이 정상적으로 생성되었는지 확인
- [ ] **운영 시간 확인**: 사용자가 적은 시간대에 적용 계획
- [ ] **롤백 계획 수립**: 문제 발생 시 즉시 롤백할 수 있는 계획 준비
- [ ] **팀원 통보**: 마이그레이션 진행 시간과 예상 영향 범위 공지

##### 적용 순서

###### 1. 운영 환경 접근
```bash
# K8s 설정
export KUBECONFIG=~/.kube/topzone.iptime.org.config

# 운영 환경 네임스페이스 확인
kubectl get pods -n devops
```

###### 2. 데이터베이스 백업
```bash
# bastion에서 운영 DB 백업
kubectl exec -it -n devops-dev bastion -- bash -c "
pg_dump -h devops-postgres-postgresql.devops.svc.cluster.local -U admin -d drillquiz > /data/operational_backup_$(date +%Y%m%d_%H%M%S).sql
"

# 백업 파일 확인
kubectl exec -it -n devops-dev bastion -- ls -la /data/operational_backup_*.sql
```

###### 3. 마이그레이션 적용
```bash
# 운영 환경 포드에 접근
kubectl exec -it -n devops [django-pod-name] -- bash

# 가상환경 활성화 및 마이그레이션 실행
source venv/bin/activate
python manage.py migrate quiz
```

###### 4. 데이터 검증
```bash
# Django shell에서 데이터 확인
python manage.py shell

# 데이터 검증 스크립트 실행
from quiz.models import Study, Exam, Question
print(f"Study with title_ko: {Study.objects.filter(title_ko__isnull=False).count()}")
print(f"Exam with title_ko: {Exam.objects.filter(title_ko__isnull=False).count()}")
print(f"Question with title_ko: {Question.objects.filter(title_ko__isnull=False).count()}")
```

##### 문제 발생 시 롤백 방법

###### 1. 마이그레이션 롤백
```bash
# 마이그레이션 되돌리기
python manage.py migrate quiz 0048
```

###### 2. 데이터베이스 복원 (최후의 수단)
```bash
# 백업 파일로 복원
psql -U admin -d drillquiz < /data/operational_backup_YYYYMMDD_HHMMSS.sql
```

##### 검증 체크리스트

###### 백엔드 검증
- [ ] Django 마이그레이션 성공
- [ ] 다국어 필드에 데이터 정상 복사
- [ ] API 응답 정상

###### 프론트엔드 검증
- [ ] study-management 페이지 정상 표시
- [ ] 제목이 "No Title"이 아닌 실제 제목으로 표시
- [ ] 언어 전환 시 적절한 언어로 표시

###### 데이터 무결성 검증
- [ ] 모든 Study의 title_ko, title_en 필드에 데이터 존재
- [ ] 모든 Exam의 title_ko, title_en 필드에 데이터 존재
- [ ] 모든 Question의 title_ko, title_en 필드에 데이터 존재

### 2. 데이터베이스 복구 로그

#### 발생했던 주요 문제점들

##### 1. 마이그레이션 순서 오류 (치명적)
```python
# ❌ 잘못된 순서 (원본 0043 마이그레이션)
operations = [
    migrations.RunPython(migrate_existing_study_data, reverse_migrate),  # 필드가 없는데 데이터 복사 시도
    migrations.AddField(...),  # 필드 추가
    migrations.RemoveField(...),  # 기존 필드 제거
]
```

**문제**: `RunPython`이 `AddField` 전에 실행되어 존재하지 않는 필드에 데이터 복사 시도
**결과**: 데이터 마이그레이션 실패, 기존 데이터 손실

##### 2. 롤백 불가능 상황
```sql
-- quiz_exam.title 컬럼에 null 값이 있어서 롤백 실패
psycopg2.errors.NotNullViolation: column "title" of relation "quiz_exam" contains null values
```

**문제**: 일부 테이블에 null 값이 있어서 마이그레이션 롤백 불가
**결과**: 데이터 복구를 위한 다른 방법 필요

##### 3. 환경 변수 설정 복잡성
```bash
# 로컬에서 K8s DB 접근 시 필요한 환경 변수들
export KUBECONFIG=~/.kube/topzone.iptime.org.config
export USE_DOCKER=true
export POSTGRES_HOST=localhost
export POSTGRES_PORT=63671  # 포트포워딩된 포트
export POSTGRES_DB=drillquiz
export POSTGRES_USER=admin
export POSTGRES_PASSWORD='DevOps!323'
```

**문제**: 환경 변수 설정이 복잡하고 실수하기 쉬움
**결과**: 연결 실패로 인한 디버깅 지연

#### 해결 방법 및 교훈

##### 1. 마이그레이션 순서 수정 (핵심)
```python
# ✅ 올바른 순서
operations = [
    # 1단계: 다국어 필드 추가
    migrations.AddField(...),
    
    # 2단계: 데이터 마이그레이션 (이제 필드가 존재함)
    migrations.RunPython(migrate_existing_study_data, reverse_migrate),
    
    # 3단계: 기존 필드 제거
    migrations.RemoveField(...),
    
    # 4단계: 인덱스 추가
    migrations.AddIndex(...),
]
```

##### 2. 단계별 마이그레이션 전략
```python
# 1단계: 0044_migrate_existing_study_data.py (데이터 복사만)
# 2단계: 0045_remove_old_fields.py (필드 제거)
# 3단계: 0046_add_indexes.py (인덱스 추가)
```

##### 3. 데이터 무결성 검증
```python
def verify_migration_success(apps, schema_editor):
    """마이그레이션 성공 여부 검증"""
    Study = apps.get_model('quiz', 'Study')
    for study in Study.objects.all():
        if not study.title_ko and not study.title_en:
            raise ValueError(f"Study {study.id}: 다국어 제목이 없습니다!")
```

### 3. 주의사항

#### 절대 하지 말아야 할 것
- ❌ **백업 없이 마이그레이션 실행**
- ❌ **사용자 많은 시간대에 적용**
- ❌ **롤백 계획 없이 진행**
- ❌ **테스트 환경에서 검증하지 않고 운영 환경에 직접 적용**

#### 반드시 해야 할 것
- ✅ **전체 데이터베이스 백업**
- ✅ **단계별 검증**
- ✅ **문제 발생 시 즉시 중단**
- ✅ **상세한 로그 기록**

### 4. 현재 상태 (2025-08-18)

#### 개발 환경 (devops-dev)
- ✅ 다국어 필드 모두 추가됨
- ✅ 기존 데이터가 다국어 필드로 복사됨
- ✅ study-management 페이지 정상 작동
- ✅ 총 7개 Study, 13개 Exam, 3050개 Question 데이터 존재

#### 운영 환경 (devops)
- ⏳ 다국어 마이그레이션 대기 중
- ⚠️ 동일한 문제 발생 가능성 있음
- 🔄 안전한 마이그레이션 계획 필요

### 5. 성능 최적화 현황

#### API 최적화 완료
- **시험 결과 API**: `/api/exam-results/summary/` 엔드포인트 추가
- **데이터 크기 감소**: 50-70% 네트워크 트래픽 감소
- **응답 시간 개선**: 평균 500ms 이내 달성

#### 번역 시스템 최적화 완료
- **배치 번역**: 200개 문제 처리 시간 20-40분 → 1-2분으로 단축
- **API 호출 감소**: 800회 → 8-12회로 대폭 감소
- **비용 절감**: OpenAI API 비용 90% 이상 절감

#### 데이터베이스 최적화 완료
- **쿼리 최적화**: `select_related`, `only()` 적용
- **인덱스 추가**: 자주 사용되는 필드에 복합 인덱스
- **캐싱 시스템**: Redis 클러스터 구축

### 6. 향후 개선 계획

#### 단기 계획 (1-3개월)
- **비동기 처리**: Celery를 사용한 백그라운드 번역 처리
- **캐싱 강화**: 번역 결과 캐싱으로 중복 번역 방지
- **모니터링**: APM 도구를 통한 성능 모니터링

#### 중기 계획 (3-6개월)
- **다중 번역 엔진**: OpenAI 외 다른 번역 서비스 지원
- **자동 스케일링**: HPA를 통한 자동 수평 확장
- **데이터베이스 샤딩**: 대용량 데이터 처리 최적화

#### 장기 계획 (6-12개월)
- **마이크로서비스**: 서비스별 독립적 확장
- **AI 기반 최적화**: 머신러닝을 통한 성능 자동 최적화
- **글로벌 확장**: CDN 및 다중 리전 배포

---

## 📋 결론

DrillQuiz는 현대적인 웹 기술 스택을 활용하여 효율적인 퀴즈 학습 플랫폼을 제공하는 종합적인 시스템입니다. 다국어 지원, 개인화된 학습 경험, 협력 학습 환경을 통해 사용자들의 학습 효과를 극대화하는 것을 목표로 합니다.

### 핵심 성공 요소
1. **사용자 중심 설계**: 직관적이고 사용하기 쉬운 인터페이스
2. **기술적 우수성**: 현대적인 기술 스택과 모범 사례 적용
3. **확장성**: 사용자 증가에 따른 시스템 확장 가능성
4. **안정성**: 높은 가용성과 데이터 보호
5. **다국어 지원**: 글로벌 사용자 접근성

이 기술 명세서는 DrillQuiz 시스템의 모든 기술적 측면을 포괄하며, 개발팀이 일관되고 고품질의 시스템을 구축하고 유지보수할 수 있도록 상세한 가이드라인을 제공합니다.
